@model SciaSquash.Model.Entities.MatchDay

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>MatchDay</h4>
    <hr />
    <table class="table">
        <tr>
            <th>Name</th>
        </tr>
        @foreach (var item in Model.Matches)
        {
            <tr>
                <td>
                   @* @Html.DisplayFor(modelItem => item.Player.NickName)*@
                </td>
            </tr>
        }
    </table>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.MatchDayID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
